package com.iluwatar.adapter.bridge;

/**
 * Implementor interface for the Bridge pattern.
 * Defines the interface for propulsion system implementations.
 */
public interface PropulsionSystem {

  void propel();
}
